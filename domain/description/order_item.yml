display_name: 订单项
config_key: default
structs:
    count:
        type: int(11)
        display_name: 编号
        description: 订单可读编号

relationships:
    order:
        entity: order
        type: belongs_to

    goods:
        entity: goods
        type: belongs_to

snaps:
    goods:
        structs:
            - name
